# Sessão 2026-07-11

## O que foi alterado
O fluxo de edição de aluno foi endurecido em quatro pontos: o carregamento da aplicação passou a usar `apiGet` com timeout; o envio de atualização de aluno ganhou fallback automático para GET quando o POST responde `Ação inválida`; o cliente passou a enviar `acao` como alias de compatibilidade; e o backend agora aceita atualização por chave derivada do nome, preservando turma/status atuais quando o payload vem incompleto.

## Conhecimento consolidado
No Apps Script, a atualização de aluno deve aceitar a chave derivada do cadastro e não depender de um `idaluno` separado na planilha. Para reduzir falhas de roteamento, o cliente precisa tolerar tanto `doPost` quanto `doGet` no mesmo endpoint, e o carregamento inicial não deve ficar preso em fetch sem timeout.

## Próximos passos
Se a edição continuar falhando, verificar o deploy ativo do Apps Script e confirmar se a URL publicada aponta para a versão atual do backend.

## Atualização 2026-07-11
### O que foi alterado
A lista principal deixou de exibir o código interno do aluno, o bloco de cadastro rápido foi reduzido para apenas novos alunos, o gráfico geral foi ocultado por enquanto, e a página de edição ganhou o botão de excluir ao lado do código interno com links de voltar/cancelar consistentes.

### Conhecimento consolidado
O código interno do aluno agora fica restrito à página de edição. A criação de nova turma foi removida da interface principal, e o gráfico geral permanece oculto até nova decisão de produto.

### Próximos passos
Se o fluxo de edição voltar a precisar de ajustes visuais, revisar também o espaçamento dos cartões de aluno após a remoção do código interno.
## Atualização 2026-07-11 (2)
### O que foi alterado
A camada de API do frontend ganhou fallback de leitura: quando o `GET` do Apps Script falha, o cliente tenta o mesmo carregamento por `POST` antes de desistir.

### Conhecimento consolidado
A rota de leitura do Web App não deve ser tratada como exclusivamente GET; o frontend precisa tolerar variações de comportamento do Apps Script entre deploys.

### Próximos passos
Se a leitura ainda falhar, validar o deploy ativo do Apps Script e a URL publicada usada em `APPS_SCRIPT_URL`.

