# Sessão 2026-07-11

## O que foi alterado
A edição de aluno continua em página dedicada em `aluno/editar-aluno/`, mas o fluxo de salvamento foi reforçado para funcionar mesmo quando o Web App do Apps Script redireciona a requisição POST para GET.

## Conhecimento consolidado
O cliente deve mandar `action` em minúsculas e repetir os parâmetros na query string ao salvar. O backend deve aceitar as ações de leitura e escrita em `doGet` e `doPost` para não quebrar no redirecionamento do Apps Script. O cadastro continua sendo atualizado diretamente na aba `cadastro` da planilha.

## Próximos passos
Se houver novo erro de salvamento, verificar primeiro a ação recebida na query string e depois o deploy ativo do Apps Script.
