# Sessão 2026-07-11

## O que foi alterado
A edição de aluno saiu do modal interno e passou para uma página dedicada em `aluno/editar-aluno/`. O botão **Editar** agora navega para essa rota com o `alunoId` do aluno selecionado. O salvamento foi endurecido para evitar `Ação inválida` no Apps Script.

## Conhecimento consolidado
O cadastro de aluno continua sendo atualizado diretamente na aba `cadastro` da planilha. O envio da edição usa `updatealuno` em minúsculas e também envia a ação na query string, porque o Web App do Apps Script pode responder por GET após redirecionamento. O código mostrado após `#` segue somente leitura.

## Próximos passos
Se houver nova falha de salvamento, conferir primeiro se a ação está chegando na query string e se o backend publicado recebeu a versão atualizada.
