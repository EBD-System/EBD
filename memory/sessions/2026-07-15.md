# Sessão 2026-07-15

## O que foi alterado
- O frontend passou a usar o backend PostgreSQL como fluxo padrão.
- O backend recebeu uma ponte de compatibilidade para a API antiga baseada em `action`/`acao`.
- O `init` agora devolve os campos que o frontend precisa para listas, edição e chamada.
- O modo fake local ficou disponível apenas como fallback manual via `DEV_FAKE_DATABASE = true`.

## Conhecimento consolidado
- `backend/exampleDb.json` continua sendo apenas seed do banco fake local.
- O contrato de resposta precisa manter `turmas`, `alunos`, `callsByTurma`, `inativos`, `resumoGeral`, `baseRowsCount` e `selectedTurmaId`.
- A navegação cliente deve preservar `APP_BASE_PATH` no GitHub Pages.

## Próximos passos
- Testar a integração real com a URL da API do backend.
- Conferir o login e o fluxo de chamada em produção.
