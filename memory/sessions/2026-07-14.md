# Sessão 2026-07-14

## O que foi alterado

- A integração de dados ganhou um modo de banco fake local no frontend, com alternância por `DEV_FAKE_DATABASE`.
- O seed local passou a vir de `backend/exampleDb.json` e as operações fake agora respondem no mesmo formato esperado pela interface.
- As páginas e a camada de API foram ajustadas para não dependerem da URL real quando o modo fake estiver ativo.

## Conhecimento consolidado

- O PostgreSQL continua sendo a fonte oficial.
- O frontend pode operar temporariamente sem backend real usando o banco fake local.
- O modo fake precisa manter os mesmos formatos de resposta que a interface já consome.

## Próximos passos

- Quando o backend real ficar pronto, desligar `DEV_FAKE_DATABASE` e validar se as respostas continuam compatíveis.


## Banco fake local
Correção do carregamento do seed fake para usar URL absoluta e ignorar estado local vazio que pudesse travar a interface sem turmas.
