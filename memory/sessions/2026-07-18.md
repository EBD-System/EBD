# Sessão 2026-07-18

## O que foi alterado

- Criado um contexto compartilhado de testes em `src/shared/flow-context.js` para armazenar JWT, usuário, payload do login e também as turmas padrão carregadas após a autenticação.
- Adicionado um fluxo oficial de login com `teste` / `123456` validando envelope HTTP, token JWT, tenant correto e reutilização do auth em uma requisição subsequente.
- Adicionado o subfluxo que consulta as 6 turmas padrão do cadastro autenticado e guarda seus ids/nome/tenant para os próximos testes.
- Registrada a estratégia de reaproveitamento do auth e das turmas no contexto compartilhado.

## Conhecimento consolidado

- Fluxos encadeados de teste autenticado devem guardar o token em um módulo compartilhado para permitir subtestes dependentes.
- O login oficial do fluxo de testes usa o usuário `teste` com senha `123456`.
- O token emitido precisa carregar `id_cadastro` e continuar válido para a próxima requisição autenticada.
- As 6 turmas padrão do cadastro autenticado são: `Crianças Menores`, `Crianças Maiores`, `Adolescentes`, `Jovens`, `Senhores` e `Senhoras`.
- Os ids dessas turmas devem ser preservados no contexto compartilhado para reutilização em testes subsequentes.

## Próximos passos

- Continuar a cadeia de testes usando o auth guardado em `src/shared/flow-context.js`.


## O que foi alterado

- Corrigida a divergência do campo `sexo` de pessoa entre backend e banco sem alterar o SQL.
- Inserida normalização de persistência em `src/modules/pessoas/repository.js` e `src/modules/auth/repository.js`, mapeando `masculino` -> `M` e `feminino` -> `F`.
- Adicionada normalização de resposta para devolver `sexo` em formato textual da API.
- Criado teste de regressão cobrindo a ida e volta do campo `sexo` entre API e banco.

## Conhecimento consolidado

- O banco de `ebd_pessoa` aceita `M`, `F`, `outro` e `nao_informado`.
- A API deve continuar aceitando os aliases textuais de pessoa e converter para os códigos esperados na persistência.
- As respostas da API para pessoa devem retornar o valor textual esperado pelos clientes, não o código cru do banco.

## Próximos passos

- Reutilizar a normalização de `sexo` em qualquer novo fluxo que grave pessoa, inclusive fluxos futuros de cadastro vinculado.
