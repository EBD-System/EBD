# Banco de dados

O arquivo `ebd_neon.sql` é uma cópia de referência do schema e dos dados usados para orientar os endpoints desta API.

A evolução do schema para multi-tenant inclui `ebd_cadastro` como raiz de cadastro e o vínculo de `ebd_classe` ao tenant correspondente.
